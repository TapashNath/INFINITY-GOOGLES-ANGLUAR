export interface Fature {
  id: string;
  name: string;
  status: boolean;
}

export interface Category {
  id: string;
  image: string;
  name: string;
  status: boolean;
}

export interface Size {
  id: string;
  name: string;
  status: boolean;
}

